import { InteractiveRow } from './interactive-row';

describe('InteractiveRow', () => {
  it('should create an instance', () => {
    const directive = new InteractiveRow();
    expect(directive).toBeTruthy();
  });
});
